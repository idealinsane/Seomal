# Seomal 실행 방법

## 요구사항

- Node.js (npx 사용)

## 실행

```bash
npx serve -p 3000
```

브라우저에서 `http://localhost:3000` 접속

## 옵션

- `?src=accounting` — `data/accounting.json` 로드
- `?i=<노드ID>` — 특정 노드에 포커스
